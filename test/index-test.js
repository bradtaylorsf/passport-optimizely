var vows = require('vows');
var assert = require('assert');
var util = require('util');
var optimizely = require('..');

vows.describe('passport-optimizely').addBatch({
  'module': {
  },
}).export(module);
